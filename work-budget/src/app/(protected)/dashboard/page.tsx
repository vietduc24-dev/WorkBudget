import { DashboardRoot } from "@/components/ui";

export const metadata = {
  title: "Dashboard",
  description: "Task management dashboard mockup.",
};

export default function Dashboard() {
  return <DashboardRoot />;
}
